{
  "name": "@open-tokyo-world/observation-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "check:deploy": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {"wrangler": "4.129.0"}
}
