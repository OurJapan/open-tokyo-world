# 参加方法

Blenderを使えなくても参加できます。ここで示す自動受付・CIは移行後の運用案です。

| 役割 | お願いすること | Blender |
|---|---|---|
| Observer | どこがいつの現実と違うかを示す | 不要 |
| Reporter | 場所、視点、違い、期待、資料URL・撮影日をIssueへ | 不要 |
| Reviewer | 同じ視点のBefore/Afterと根拠を比較する | 不要 |
| 3D Contributor | 必要な部位を編集し、ID・出典・再現手順を残す | 必要な場合のみ |
| AI Agent | 調査、対象特定、実装、render、検証、PR | 自動実行 |
| Maintainer | scope・権利・予算・merge判断 | 必須ではない |

## 報告する

[現実との差分テンプレート](issues/templates/reality-difference.md)を使います。建物名か地図リンクがあればよく、正確な座標・object IDはAgentが補います。「いつ撮った写真か」「どの方向から見たか」が分かると調査しやすくなります。

写真は自分が提供できるものに限り、撮影者・撮影日・利用条件を記載してください。他者の公式写真はリンクだけでも役立ちます。個人情報や第三者の顔・車両番号が不要なら含めない資料を使ってください。Street Viewリンクを示す場合も、その画像を自動的にモデリングへ使う許可としては扱いません。

## レビューする

PRの最初に、対象地物・短い変更理由・同一カメラのBefore/Afterを掲載します。確認するのは「資料と合うか」「周辺を壊していないか」「推定や未確認が明示されているか」です。気になる箇所はカメラIDと画像内の位置を示し、追加指示を投稿します。AIの説明に納得できなければ承認する必要はありません。

## コード・3D資産を編集する

一つのIssue/featureを基本単位にし、地物定義と生成コードの修正を優先します。手編集なら独立asset、入力hash、変更部位、使った資料を残します。既存IDをリネームで失わず、対象外のgeometry・camera・照明を一緒に変更しません。

提出には根拠、変更手順、同条件の比較画像、Validation結果、実行headを含めます。大きなデータはmanifestで参照し、連番動画やcacheをGitへ追加しません。直接編集者にもAgentと同じ品質条件を適用します。

ライセンス採用前に第三者素材へ勝手にライセンスを付けないでください。公開時のcode/asset別条件は [データ方針](docs/data-sources.md) に従って確定します。
